vocab_size: 32
padding_idx: 1

d_model: 512
n_layers: 24
n_heads: 8
d_ffn: null
ffn_multiplier: null

max_seq_len: 320
max_timesteps: 100
use_timestep_embedding: false

dropout: 0.1
attention_dropout: 0.1
embedding_dropout: 0.1
